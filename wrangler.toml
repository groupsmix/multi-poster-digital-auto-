name = "nexus"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# ── D1 Database ──────────────────────────────────────────
[[d1_databases]]
binding = "DB"
database_name = "nexus-db"
database_id = "f73cd60b-1baa-4e80-8e13-afb88960051d"
migrations_dir = "migrations"

# ── KV Namespace ─────────────────────────────────────────
[[kv_namespaces]]
binding = "CACHE"
id = "9f336b013df04301894a947ab2bff16a"

# ── R2 Bucket ────────────────────────────────────────────
[[r2_buckets]]
binding = "ASSETS_BUCKET"
bucket_name = "nexus-assets"

# ── Durable Objects ──────────────────────────────────────
[durable_objects]
bindings = [
  { name = "WORKFLOW_COORDINATOR", class_name = "WorkflowCoordinator" },
  { name = "PROVIDER_ROUTER", class_name = "ProviderRouter" }
]

[[migrations]]
tag = "v1"
new_classes = ["WorkflowCoordinator"]

[[migrations]]
tag = "v2"
new_classes = ["ProviderRouter"]

# ── Environment Variables ────────────────────────────────
[vars]
ENVIRONMENT = "development"
APP_NAME = "NEXUS"
